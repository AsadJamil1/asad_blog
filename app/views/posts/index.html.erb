<% @posts.each do |post| %>
	<div class="post_wrapper">
		<h2 class="title"><%= link_to post.title, post%></h2>
		<p class="date">Posted at : <%= post.created_at.strftime("%B, %d, %Y") %> by <%= post.user.email%></p>
	</div>
<%end%>